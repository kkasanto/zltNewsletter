@page
@model             viewarticleModel

@{
    ViewData["Title"] = Model.Article.ArticleTitle;
}

    <div class="col-sm-10 article_head">
        <span>@Model.Article.ArticleTitle</span>
    </div>
    
    <div class="col-sm-10">
            @Html.Raw(Model.Article.ArticleHeader)
        </div>

        <div class="col-sm-10">
            @Html.Raw(Model.Article.ArticleText)
        </div>


<div>
    <a asp-page="./Edit" asp-route-id="@Model.Article.ArticleId">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
